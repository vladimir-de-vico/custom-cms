import { NextLinkWrapper } from '@/lib/components/next-link-wrapper/next-link-wrapper';
import type { NextLinkWrapperProps } from '@/lib/components/next-link-wrapper/next-link-wrapper.types';

export const AppLink = NextLinkWrapper;
export type AppLinkProps = NextLinkWrapperProps;
